import { createFileRoute, Link, useNavigate } from "@tanstack/react-router";
import { ArrowRight } from "lucide-react";
import { AuthFooter, AuthInput, AuthMark } from "@/lib/scentlore";
import { Button } from "@/components/ui/button";

export const Route = createFileRoute("/auth/login")({ head: () => ({ meta: [{ title: "Sign in — Scentlore" }, { name: "description", content: "Sign in to continue your Scentlore journey." }, { property: "og:title", content: "Sign in — Scentlore" }, { property: "og:description", content: "Sign in to continue your Scentlore journey." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: Login });

function Login() { const navigate = useNavigate(); return <div className="auth-screen flex min-h-screen items-center justify-center bg-background px-5 py-12"><div className="w-full max-w-[420px] animate-fade-in"><AuthMark /><div className="mt-12 rounded-lg border border-border bg-card p-7 shadow-[0_18px_40px_-32px_var(--color-ink)] sm:p-9"><h1 className="font-display text-[32px] tracking-[-0.04em] text-ink">Welcome back.</h1><p className="mt-2 text-[12px] text-muted-foreground">Your scent story is waiting.</p><div className="mt-8 space-y-5"><AuthInput label="Email address" type="email" placeholder="you@example.com" /><div><div className="mb-2 flex justify-between"><span className="text-[11px] font-semibold text-ink">Password</span><Link to="/auth/forgot-password" className="text-[10px] font-semibold text-ink underline decoration-gold decoration-2 underline-offset-4">Forgot password?</Link></div><input type="password" placeholder="Enter your password" className="h-11 w-full rounded-md border border-border bg-card px-3 text-[12px] outline-none transition focus:border-gold focus:ring-2 focus:ring-gold/20" /></div><Button onClick={() => navigate({ to: "/" })} className="h-11 w-full rounded-md bg-ink text-[11px] font-semibold text-sidebar-primary-foreground hover:bg-plum">Sign in <ArrowRight className="size-3.5" /></Button></div><div className="my-7 flex items-center gap-3 text-[10px] text-muted-foreground"><span className="h-px flex-1 bg-border" /> or <span className="h-px flex-1 bg-border" /></div><Button variant="outline" className="h-11 w-full text-[11px]">Continue with Google</Button><p className="mt-7 text-center text-[11px] text-muted-foreground">New to Scentlore? <Link to="/auth/signup" className="font-semibold text-ink underline decoration-gold decoration-2 underline-offset-4">Create an account</Link></p></div><AuthFooter /></div></div>; }