import { createFileRoute, Link, useNavigate } from "@tanstack/react-router";
import { ArrowRight, Check } from "lucide-react";
import { AuthFooter, AuthInput, AuthMark } from "@/lib/scentlore";
import { Button } from "@/components/ui/button";

export const Route = createFileRoute("/auth/signup")({ head: () => ({ meta: [{ title: "Create your account — Scentlore" }, { name: "description", content: "Create your Scentlore account and begin building your scent wardrobe." }, { property: "og:title", content: "Create your account — Scentlore" }, { property: "og:description", content: "Begin building your scent wardrobe with Scentlore." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: Signup });

function Signup() { const navigate = useNavigate(); return <div className="auth-screen flex min-h-screen items-center justify-center bg-background px-5 py-12"><div className="w-full max-w-[420px] animate-fade-in"><AuthMark /><div className="mt-12 rounded-lg border border-border bg-card p-7 shadow-[0_18px_40px_-32px_var(--color-ink)] sm:p-9"><h1 className="font-display text-[32px] tracking-[-0.04em] text-ink">Make room for scent.</h1><p className="mt-2 text-[12px] text-muted-foreground">A more considered way to discover fragrance.</p><div className="mt-7 space-y-4"><AuthInput label="Your name" placeholder="Maya Wilson" /><AuthInput label="Email address" type="email" placeholder="you@example.com" /><AuthInput label="Create a password" type="password" placeholder="At least 8 characters" /><Button onClick={() => navigate({ to: "/" })} className="mt-2 h-11 w-full rounded-md bg-ink text-[11px] font-semibold text-sidebar-primary-foreground hover:bg-plum">Create account <ArrowRight className="size-3.5" /></Button></div><div className="mt-6 space-y-2 text-[10px] text-muted-foreground"><p className="flex items-center gap-2"><Check className="size-3 text-gold" /> Personalised discovery, at your pace.</p><p className="flex items-center gap-2"><Check className="size-3 text-gold" /> A private wardrobe for your favourites.</p></div><p className="mt-7 text-center text-[11px] text-muted-foreground">Already have an account? <Link to="/auth/login" className="font-semibold text-ink underline decoration-gold decoration-2 underline-offset-4">Sign in</Link></p></div><AuthFooter /></div></div>; }