import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowLeft, ArrowRight, MailCheck } from "lucide-react";
import { AuthFooter, AuthInput, AuthMark } from "@/lib/scentlore";
import { Button } from "@/components/ui/button";

export const Route = createFileRoute("/auth/forgot-password")({ head: () => ({ meta: [{ title: "Reset your password — Scentlore" }, { name: "description", content: "Reset your Scentlore password and return to your scent journal." }, { property: "og:title", content: "Reset your password — Scentlore" }, { property: "og:description", content: "Reset your Scentlore password and return to your scent journal." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: ForgotPassword });

function ForgotPassword() { return <div className="auth-screen flex min-h-screen items-center justify-center bg-background px-5 py-12"><div className="w-full max-w-[420px] animate-fade-in"><AuthMark /><div className="mt-12 rounded-lg border border-border bg-card p-7 shadow-[0_18px_40px_-32px_var(--color-ink)] sm:p-9"><Link to="/auth/login" className="inline-flex items-center gap-2 text-[11px] font-semibold text-muted-foreground hover:text-ink"><ArrowLeft className="size-3.5" /> Back to sign in</Link><div className="mt-7 grid size-12 place-items-center rounded-full bg-soft-gold text-ink"><MailCheck className="size-5" strokeWidth={1.5} /></div><h1 className="mt-6 font-display text-[32px] tracking-[-0.04em] text-ink">Find your way back.</h1><p className="mt-2 text-[12px] leading-5 text-muted-foreground">Enter your email and we'll send a quiet little nudge in the right direction.</p><div className="mt-7 space-y-5"><AuthInput label="Email address" type="email" placeholder="you@example.com" /><Button className="h-11 w-full rounded-md bg-ink text-[11px] font-semibold text-sidebar-primary-foreground hover:bg-plum">Send reset link <ArrowRight className="size-3.5" /></Button></div></div><AuthFooter /></div></div>; }